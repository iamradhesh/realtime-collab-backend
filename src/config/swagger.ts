import swaggerJsdoc from "swagger-jsdoc";

const options: swaggerJsdoc.Options = {
  definition: {
    openapi: "3.0.0",
    info: {
      title: "Real-Time Collaboration API",
      version: "1.0.0",
      description: "API for managing projects, collaborators, and real-time code execution",
    },
    servers: [
      {
        url: "http://localhost:5000",
        description: "Development server",
      },
    ],
    components: {
      securitySchemes: {
        bearerAuth: {
          type: "http",
          scheme: "bearer",
          bearerFormat: "JWT",
        },
      },
    },
  },
  apis: ["./src/routes/*.ts", "./src/modules/**/*.ts"], // Path to the API docs
};

export const swaggerSpec = swaggerJsdoc(options);